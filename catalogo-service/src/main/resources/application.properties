spring.application.name=catalogo-service

# Puerto del servicio catalogo-service
server.port=9091

# Conexion a la base de datos db_catalgo, si no existe se crea automaticamente
spring.datasource.url=jdbc:mysql://localhost:3306/db_catalogo?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none

#Liquibase
spring.liquibase.enabled=true
# Ruta del archivo con las instrucciones para crear la tabla y los 10 registros
spring.liquibase.change-log=classpath:db/changelog/db.changelog.sql

#Logger
logging.file.name=logs/catalogo-service.log
logging.pattern.console=%d{HH:mm:ss} %-5level %msg%n
logging.pattern.file=%d{HH:mm:ss} %-5level %msg%n